<!DOCTYPE html>
<html>
    <head>
        <meta charset="utf-8">
        <title></title>
    </head>
    <body>
      <p>Favorites</p>

      <ul>
        <% for (let i = 0; i < places.length; i++) {%>
          <p><%= places[i].city%></p>

          <% if (places[i].imgURL) {%>
            <a href="/travel/<%= places[i].id%>"><img src=<%= places[i].imgURL%> ></a>

          <% } else { %>
            <a href="/travel/<%= places[i].id%>"><img src="https://imgplaceholder.com/420x320/ff7f7f/333333/fa-image"></a>
          <% } %>
        <% } %>
      </ul>
    </body>
</html>
